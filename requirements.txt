fastapi>=0.115
uvicorn[standard]>=0.30
Pillow>=10.0  # decode incoming JPEG frames to read width/height for camera stats
ultralytics>=8.3  # YOLOv8n object detection (pulls torch; auto-downloads weights on first run)
